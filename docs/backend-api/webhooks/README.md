# Webhooks Router

Base path: `/api/v1/webhooks`

This router receives signed backend events from third-party systems.

## Features

- [LiveKit](./livekit.md): receive and verify LiveKit webhook events, auto-sync egress status
